{
    "max_entries": {
        "description": "单 scope 最大记忆条数",
        "type": "int",
        "hint": "超过此数量后会按置信度+访问频率淘汰低质量记忆。每个用户/群聊独立计算。",
        "default": 500
    },
    "min_confidence": {
        "description": "最低置信度阈值",
        "type": "float",
        "hint": "低于此置信度的记忆会被优先淘汰（0.0~1.0）",
        "default": 0.3
    },
    "enable_active_learn_hint": {
        "description": "启用主动学习提示",
        "type": "bool",
        "hint": "当消息命中'什么是/不懂/解释一下'等关键词且记忆库无答案时，提示 LLM 学习",
        "default": true
    },
    "learn_weight": {
        "description": "主动学习强度 (0~1)",
        "type": "float",
        "hint": "0=关闭；0.1~0.4=温和提示（\"可考虑调用\"）；0.5~0.7=默认（\"建议调用\"）；0.8~1.0=强提示（\"请立即调用\"）。值越大 LLM 越倾向执行 search_and_learn。",
        "default": 0.7
    },
    "search_top_k": {
        "description": "工具搜索返回条数",
        "type": "int",
        "hint": "search_and_learn / memory_search 返回的最大搜索结果数（1~20）。越多 LLM 看的信息越全，但占用上下文也越多。",
        "default": 5
    },
    "default_confidence": {
        "description": "新条目标置信度",
        "type": "float",
        "hint": "手动导入或未经 LLM 精炼时，新记忆的默认置信度（0.1~1.0）。精炼后会用 LLM 评估值覆盖。",
        "default": 0.6
    },
    "chunk_size": {
        "description": "导入分块大小（字符）",
        "type": "int",
        "hint": "导入长文档时每块的最大字符数。越大上下文越完整但可能超过模型窗口。",
        "default": 500
    },
    "chunk_overlap": {
        "description": "导入分块重叠（字符）",
        "type": "int",
        "hint": "相邻分块之间重叠的字符数，避免切分导致断裂。",
        "default": 50
    },
    "enable_bilibili": {
        "description": "启用 B 站搜索源",
        "type": "bool",
        "hint": "需要自行 pip install bilibili-api-python。不可用时 search_bilibili 工具会回退到普通网页搜索（site:bilibili.com）",
        "default": false
    },
    "verifier_search_source": {
        "description": "验证搜索源",
        "type": "string",
        "options": ["auto", "web", "bilibili", "web+bilibili", "llm"],
        "hint": "验证记忆时使用的搜索源。auto=有API就用网页搜索+B站，没API则LLM；web=仅网页搜索(Tavily/BoCha/Brave)；bilibili=仅B站；web+bilibili=网页+B站；llm=纯LLM验证不搜索。",
        "default": "auto"
    },
    "enable_web_search": {
        "description": "启用联网搜索",
        "type": "bool",
        "hint": "关闭后 search_and_learn 等联网搜索工具将不可用，插件仅依赖本地记忆库。",
        "default": true
    },
    "external_search_first_result_grace_seconds": {
        "description": "首个搜索结果后的等待窗口（秒）",
        "type": "float",
        "hint": "任一外部搜索源首次返回非空结果后，最多再等待其它来源的秒数，随后取消慢源。",
        "default": 0.5
    },
    "web_search_only_highest_priority": {
        "description": "联网搜索仅使用最高优先级来源",
        "type": "bool",
        "hint": "开启后，搜索/验证时仅使用「知识搜索源优先级」中排第一位的来源，不再混合其它来源。",
        "default": false
    },
    "knowledge_source_priority": {
        "description": "知识搜索源优先级",
        "type": "string",
        "hint": "逗号分隔外部搜索源，例如 'web,bilibili'。排在前面的源优先级更高；开启「仅最高优先级」时只使用第一个来源。本地记忆默认始终优先检索，不需要放在此处。",
        "default": "web,bilibili"
    },
    "knowledge_domain_scope": {
        "description": "知识领域范围",
        "type": "string",
        "hint": "逗号分隔的兴趣领域白名单，例如 '游戏,动漫,编程'。配合「允许跨领域回复」开关使用；为空时表示不限制。",
        "default": ""
    },
    "enable_cross_domain": {
        "description": "允许跨领域回复",
        "type": "bool",
        "hint": "关闭后，当用户问题未命中「知识领域范围」时，插件会提示 LLM 明确回复不知道/没玩过/没见过等。",
        "default": true
    },
    "cross_domain_exclude_admin": {
        "description": "拒绝跨领域搜索时排除管理员",
        "type": "bool",
        "hint": "开启后，管理员不受跨领域限制，仍可触发联网搜索；关闭则管理员也会被限制。",
        "default": true
    },
    "debate_rounds": {
        "description": "质疑验证的自辩论轮数",
        "type": "int",
        "hint": "验证某条记忆时，LLM 自辩论的轮数。2 表示：支持方→质疑方→仲裁。",
        "default": 2
    },
    "llm_provider_id": {
        "description": "插件专用 LLM Provider（留空用当前对话模型）",
        "type": "string",
        "_special": "select_provider",
        "hint": "选择本插件进行搜索学习 / 导入精炼 / 验证 时使用的 LLM。留空则回退到 AstrBot 当前对话模型。Dashboard「⚙ 设置」页的同名字段优先级更高。",
        "default": ""
    },
    "embedding_enabled": {
        "description": "启用向量检索",
        "type": "bool",
        "hint": "无可用 Embedding Provider 时自动降级为纯 FTS5。需要先在 AstrBot 中配置 Embedding Provider。",
        "default": true
    },
    "hybrid_search_weight": {
        "description": "FTS5 与向量分数权重",
        "type": "string",
        "hint": "格式 'fts,vec'，例如 '0.4,0.6' 表示 FTS5 占 40%，向量占 60%",
        "default": "0.4,0.6"
    },
    "decay_half_life_days": {
        "description": "记忆衰减半衰期（天）",
        "type": "int",
        "hint": "记忆分数随访问时间衰减，半衰期越短遗忘越快。30 天表示 30 天未访问则分数减半。",
        "default": 30
    },
    "enable_scope_fallback": {
        "description": "启用跨 scope 回退检索",
        "type": "bool",
        "hint": "private scope 命中不足时回退到 group/global，带 0.8/0.6 分数惩罚",
        "default": true
    },
    "priority_topics": {
        "description": "关心的领域（优先检索）",
        "type": "string",
        "hint": "逗号分隔，例如 'Python,量子计算,历史'。topic 或 keywords 命中任一关键词的记忆会获得 1.3x 分数加权，优先注入。点击 Dashboard「🎯 主动学习关心领域」按钮可批量学习这些领域。",
        "default": ""
    },
    "auto_learn_topic_limit": {
        "description": "主动学习关心领域的上限条数",
        "type": "int",
        "hint": "点击「🎯 主动学习关心领域」按钮时，最多学习多少条记忆。每个关心领域均分配额。范围 1-500。",
        "default": 100
    },
    "context_inject_count": {
        "description": "上下文注入记忆条数",
        "type": "int",
        "hint": "每次对话最多注入 3 条记忆；同时受总约 1800 字符、单条约 700 字符预算限制。为兼容旧配置，大于 3 的值会自动收紧为 3。",
        "default": 3
    },
    "priority_boost_max": {
        "description": "关心领域初始/命中时分数乘子",
        "type": "float",
        "hint": "关心领域命中时的分数乘子。1.3 表示 +30% 加权。1.0 等于关闭。建议 1.1-1.5。",
        "default": 1.3
    },
    "priority_boost_min": {
        "description": "关心领域衰减下限",
        "type": "float",
        "hint": "连续非关心领域查询时 boost 逐步衰减到此下限。1.0 表示完全淡化（等于无优先）。",
        "default": 1.0
    },
    "priority_boost_decay": {
        "description": "关心领域每次未命中的衰减系数",
        "type": "float",
        "hint": "每次检索结果未命中关心领域时，boost 乘以此系数。0.85 表示每次衰减 15%。越大衰减越慢。",
        "default": 0.85
    },
    "enable_slang_capture": {
        "description": "启用群黑话被动捕获",
        "type": "bool",
        "hint": "自动扫描群消息提取候选黑话词（不调 LLM），攒够 batch_size 后定时 1 次 LLM 批量学习。需要 AstrBot 支持 on_message 钩子。",
        "default": false
    },
    "admin_ids": {
        "description": "管理员 QQ 号（逗号分隔）",
        "type": "string",
        "hint": "只有此名单内的用户消息才会触发主动学习（search_and_learn）。留空则所有人可学。同时会读取 AstrBot 全局配置的 wl_admin。",
        "default": ""
    },
    "slang_capture_interval_hours": {
        "description": "黑话批量学习间隔（小时）",
        "type": "int",
        "hint": "每 scope 距上次批量学习多少小时后才允许再次触发。",
        "default": 24
    },
    "slang_capture_batch_size": {
        "description": "每次批量学习的候选词数",
        "type": "int",
        "hint": "pending 候选词达到此数量才触发批量学习。每次 1 次 LLM 调用处理这么多词。",
        "default": 5
    },
    "slang_capture_min_occurrences": {
        "description": "候选词最低出现次数",
        "type": "int",
        "hint": "候选词在群里出现次数低于此值则不计入批量学习。避免一次性口语被学习。",
        "default": 2
    },
    "slang_capture_scope_only_group": {
        "description": "仅捕获群聊消息",
        "type": "bool",
        "hint": "true 仅捕获 group scope；false 同时捕获 private scope 黑话。",
        "default": true
    }
}
